# Lint rules

- Test rules with both rejected examples and valid neighboring patterns.
- Execution-boundary exceptions must name exact files. Remove exceptions when their implementation is deleted.
- Never weaken application type checks to accommodate JavaScript plugin internals.

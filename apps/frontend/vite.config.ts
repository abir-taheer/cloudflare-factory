import { frontendViteConfiguration } from "./src/node-vite-config.js";

// eslint-disable-next-line import/no-default-export -- Vite discovers configuration through the default export.
export default frontendViteConfiguration();
